version: '2.4'

networks:
  proxy:
    external: true

services:
  tang:
    hostname: tang
    image: padhihomelab/tang:git.0867603d_git.786b426d
    restart: unless-stopped

    security_opt:
    - no-new-privileges:true

    networks:
    - proxy
    volumes:
    - ./data/tang/db:/db:rw

    environment:
      DOCKER_UID: ${USER_ID:?}
