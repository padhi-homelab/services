name: "Docker Compose Test (AirDC++)"

on:
  pull_request:
    branches: master
  push:
    branches: master
  schedule:
  - cron: "0 8 * * *"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      -
        name: "Git: Checkout"
        uses: actions/checkout@v2
      -
        name: "Service: Setup"
        run: |
          mkdir -p airdcpp/data/openvpn
          cp -r .github/workflows/config/vpn airdcpp/data/openvpn/config
      -
        name: "Service: Start"
        run: |
          ./service.sh up --no-override airdcpp
      -
        name: "Service: Wait"
        run: |
          sleep 15s
      -
        name: "Service: Test"
        run: |
          cd airdcpp
          REPLY="$(docker inspect --format '{{json .State.Health.Status}}' $(docker-compose ps -q airdcpp))"
          [[ "$REPLY" == '"healthy"' ]] || (docker-compose logs && exit 1)
