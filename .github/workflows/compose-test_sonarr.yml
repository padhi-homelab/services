name: "Docker Compose Test (Sonarr)"

on:
  pull_request:
    branches: master
  push:
    branches: master
  schedule:
  - cron: "0 8 * * *"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      -
        name: "Git: Checkout"
        uses: actions/checkout@v2
      -
        name: "Service: Start"
        run: |
          ./service.sh up --no-override sonarr
      -
        name: "Service: Wait"
        run: |
          sleep 60s
      -
        name: "Service: Test"
        run: |
          cd sonarr
          REPLY="$(docker inspect --format '{{json .State.Health.Status}}' $(docker-compose ps -q sonarr))"
          [[ "$REPLY" == '"healthy"' ]] || (docker-compose logs && exit 1)
