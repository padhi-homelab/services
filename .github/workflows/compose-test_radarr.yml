name: "Docker Compose Test (Radarr)"

on:
  pull_request:
    branches: master
  push:
    branches: master
  schedule:
  - cron: "0 8 * * *"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      -
        name: "Git: Checkout"
        uses: actions/checkout@v2
      -
        name: "Service: Start"
        run: |
          ./service.sh up --no-override radarr
      -
        name: "Service: Wait"
        run: |
          sleep 45s
      -
        name: "Service: Test"
        run: |
          cd radarr
          REPLY="$(docker inspect --format '{{json .State.Health.Status}}' $(docker-compose ps -q radarr))"
          [[ "$REPLY" == '"healthy"' ]] || (docker-compose logs && exit 1)
