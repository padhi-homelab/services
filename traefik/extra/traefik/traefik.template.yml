accessLog:
  format: json
  filters:
    statusCodes:
    - "400-499"
    - "500-599"
    minDuration: "5s"
  fields:
    defaultMode: drop
    names:
      Duration: keep
      RouterName: keep
      ServiceName: keep
      ClientAddr: keep
      RequestAddr: keep
      RequestPath: keep
      RequestProtocol: keep
      RequestScheme: keep
      RequestContentSize: keep
      OriginStatus: keep
      DownstreamStatus: keep
      Overhead: keep
      RetryAttempts: keep
      TLSVersion: keep
    headers:
      defaultMode: drop
      names:
        Referer: keep
        User-Agent: keep

api:
  dashboard: true
  insecure: false
  basePath: /${TRAEFIK_UI_BASE_PATH}

entryPoints:
  x-internal:
    address: ":8080"
  lan-http-dbg:
    address: ":${SERVER_LAN_HTTP_DEBUG_PORT}"
    http:
      middlewares:
      - restrict-to-lan-ip@file
      - default@file
  lan-http-dbg-exc:
    address: ":${SERVER_LAN_HTTP_DEBUG_PORT_EXCLUSIVE}"
    http:
      middlewares:
      - restrict-to-lan-ip@file
      - default@file
  lan-https-dbg:
    address: ":${SERVER_LAN_HTTPS_DEBUG_PORT}"
    http:
      middlewares:
      - restrict-to-lan-ip@file
      - default@file
      - add-ssl-headers@file
      tls: true
  lan-https-dbg-exc:
    address: ":${SERVER_LAN_HTTPS_DEBUG_PORT_EXCLUSIVE}"
    http:
      middlewares:
      - restrict-to-lan-ip@file
      - default@file
      - add-ssl-headers@file
      tls: true
  lan-http-svc:
    address: ":${SERVER_LAN_HTTP_SERVICE_PORT}"
    http:
      middlewares:
      - restrict-to-lan-ip@file
      - default@file
  lan-http-svc-exc:
    address: ":${SERVER_LAN_HTTP_SERVICE_PORT_EXCLUSIVE}"
    http:
      middlewares:
      - restrict-to-lan-ip@file
      - default@file
  lan-https-svc:
    address: ":${SERVER_LAN_HTTPS_SERVICE_PORT}"
    http:
      middlewares:
      - restrict-to-lan-ip@file
      - default@file
      - add-ssl-headers@file
      tls: true
  lan-https-svc-exc:
    address: ":${SERVER_LAN_HTTPS_SERVICE_PORT_EXCLUSIVE}"
    http:
      middlewares:
      - restrict-to-lan-ip@file
      - default@file
      - add-ssl-headers@file
      tls: true
  wan-https-dbg:
    address: ":${SERVER_WAN_HTTPS_DEBUG_PORT}"
    http:
      middlewares:
      - block-ddos-attacks@file
      - default@file
      - add-ssl-headers@file
      tls:
        domains:
        - main: ${SERVER_WAN_FQDN}
  wan-https-dbg-exc:
    address: ":${SERVER_WAN_HTTPS_DEBUG_PORT_EXCLUSIVE}"
    http:
      middlewares:
      - block-ddos-attacks@file
      - default@file
      - add-ssl-headers@file
      tls:
        domains:
        - main: ${SERVER_WAN_FQDN}
  wan-https-svc:
    address: ":${SERVER_WAN_HTTPS_SERVICE_PORT}"
    http:
      middlewares:
      - block-ddos-attacks@file
      - default@file
      - add-ssl-headers@file
      tls:
        domains:
        - main: ${SERVER_WAN_FQDN}
  wan-https-svc-exc:
    address: ":${SERVER_WAN_HTTPS_SERVICE_PORT_EXCLUSIVE}"
    http:
      middlewares:
      - block-ddos-attacks@file
      - default@file
      - add-ssl-headers@file
      tls:
        domains:
        - main: ${SERVER_WAN_FQDN}

global:
  checkNewVersion: false
  sendAnonymousUsage: false

log:
  format: json
  level: INFO

ping:
  entryPoint: x-internal

providers:
  docker:
    endpoint: "tcp://docker.sock-proxy:1279"
    exposedByDefault: false
    network: traefik-net
    watch: true
  file:
    directory: "/dynamic"
    watch: true

serversTransport:
  insecureSkipVerify: true
