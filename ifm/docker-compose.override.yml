version: '2.4'

services:
  caddy:
    ports:
    - 10443:10443
  ifm:
    volumes:
    - ./config/ifm/auth.htpasswd:/auth.htpasswd:ro    # default password is "password"
  
    environment:
      IFM_AUTH: 1
      IFM_AUTH_SOURCE: file;/auth.htpasswd
